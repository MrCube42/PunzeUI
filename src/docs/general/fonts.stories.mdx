import { Meta,Typeset } from '@storybook/addon-docs';

<Meta title="Punze UI / General / Fonts" parameters={{ order: 20 }} />

# Fonts

Basic Font: **Roboto**

Weights: 400 (regular), 700 (bold)

<Typeset fontFamily={'Roboto'} fontSizes={[ Number(16), Number(18), Number(36) ]} fontWeight={400} />